const slots=[...document.querySelectorAll('.section-slot')];Promise.all(slots.map(async s=>{const id=s.dataset.section;const r=await fetch(id+'.html');s.outerHTML=await r.text();})).then(()=>{const input=document.getElementById('searchInput');const sections=[...document.querySelectorAll('main section')];input.addEventListener('input',()=>{const q=input.value.trim().toLowerCase();sections.forEach(s=>{const hay=(s.innerText+' '+(s.dataset.search||'')).toLowerCase();s.classList.toggle('hidden',q&&!hay.includes(q));});});});